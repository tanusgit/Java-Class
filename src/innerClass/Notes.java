package innerClass;

public class Notes {
/*
 * Inner class: Creating a class inside a class
 * We have 4 types of inner classes
 * 	Member inner class
	static inner class
	Anonymous inner class
	Local inner class
	
	Member inner class:

	public class Outer{
 	class Inner{
 
 	}
	}
	Outer o = new Outer();

	Outer.Inner O1 = o.new Inner();
	
	
	inner class can access anything from outer class. 
 */
}
