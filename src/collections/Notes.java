package collections;

import java.util.Collections;

public class Notes {
	/*
	 * collection has a sort method to sort the collections
	 * we have set, list and map in collection
	 * syntax to use the sort() is  Collections.sort(l);
	 * 
	 */

}
